# GTX 980 Ti (Maxwell Architecture) Optimized Requirements
# Compatible with CUDA 11.2.2, cuDNN 8.2.1

# Web Framework - Streamlit Dashboard
streamlit>=1.20.0,<1.28.0

# REST API - FastAPI for Android clients
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# OCR Libraries
paddleocr>=2.6.1

# PaddlePaddle version constraints:
# - Docker (Python 3.8, CUDA 11.2): Uses paddlepaddle-gpu==2.6.1.post112 (installed in Dockerfile)
# - Local venv (Python 3.13, CPU only): Uses paddlepaddle==3.0.0
# 
# NOTE: PaddlePaddle 3.3.0 has OneDNN compatibility issues causing:
#   "ConvertPirAttribute2RuntimeAttribute not support [pir::ArrayAttribute<pir::DoubleAttribute>]"
# PaddlePaddle 3.0.0 works correctly with PaddleOCR 3.x models.
paddlepaddle==3.0.0; python_version >= "3.13"

# OpenCV with CUDA support (installed via Dockerfile for GPU)
# For local CPU-only venv, opencv-python-headless is sufficient
opencv-python-headless>=4.7.0; python_version >= "3.13"

# Data Processing
# Docker (Python 3.8) uses older versions for compatibility
pandas==1.5.3; python_version < '3.13'
pandas>=2.2.3; python_version >= '3.13'

numpy==1.23.5; python_version < '3.9'
numpy>=1.26.0; python_version >= '3.9'

pyarrow==11.0.0; python_version < '3.13'
pyarrow>=14.0.0; python_version >= '3.13'

# File Monitoring
watchdog>=2.2.0

# Optional: Tesseract for CPU-based recognition fallback
pytesseract>=0.3.10

# Optional: EasyOCR alternative
# easyocr>=1.6.0

# Image Processing
Pillow>=9.0.0

scipy==1.9.3; python_version < '3.13'
scipy>=1.11.0; python_version >= '3.13'

scikit-image==0.19.3; python_version < '3.13'
scikit-image>=0.22.0; python_version >= '3.13'

albumentations==1.3.1; python_version < '3.13'
albumentations>=1.4.0; python_version >= '3.13'

# HTTP Client (for API calls)
httpx>=0.24.0

# Performance
# No additional packages needed - GPU acceleration via OpenCV CUDA

# Logging
colorlog>=6.7.0

# Type hints support
typing-extensions>=4.0.0
